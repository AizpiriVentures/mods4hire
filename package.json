{
  "name": "mods4hire",
  "version": "0.1.0",
  "description": "Marketplace for hiring community moderators",
  "scripts": {
    "dev": "npx wrangler pages dev ./public",
    "deploy": "npx wrangler pages deploy ./public --project-name mods4hire"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
